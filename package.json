{
  "name": "caz-torno",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "TORNO — motor generativo de la identidad CAUCE. Guilloché reinterpretado como línea de canal.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.0.0"
  }
}
